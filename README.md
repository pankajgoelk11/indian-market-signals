# Indian market signals

Streamlit dashboard for India session timing, global overnight cues, and local benchmarks. Code is in `indian_market_signals/`.

```bash
cd indian_market_signals
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```
